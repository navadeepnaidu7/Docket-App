name: docket
description: "Docket mobile application."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.0
  flutter_riverpod: ^2.6.1
  google_fonts: ^8.1.0
  flutter_svg: ^2.0.17
  shared_preferences: ^2.5.5
  path_provider: ^2.1.5
  cryptography: ^2.7.2
  # Held at 10.x on purpose. v11 removed the RSA_ECB_PKCS1Padding /
  # AES_CBC_PKCS7Padding ciphers that 9.2.4 wrote with, so jumping 9 -> 11
  # leaves existing passport, ID, wallet-order and trash records unreadable.
  # v10 migrates that data to the new ciphers on first read; the v11 bump can
  # only land once shipped installs have run a 10.x build.
  flutter_secure_storage: ">=10.3.1 <11.0.0"
  camera: ^0.12.0+2
  google_mlkit_text_recognition: ^0.16.0
  google_mlkit_face_detection: ^0.14.0
  google_mlkit_barcode_scanning: ^0.15.0
  qr_flutter: ^4.1.0
  permission_handler: ^13.0.0
  url_launcher: ^6.3.2
  # File picking for attaching PDF documents and images manually from system storage.
  file_picker: ^8.1.7
  # Photo library access for picking document images on iOS and Android.
  image_picker: ^1.1.2
  # Renders PDF first-page thumbnails and hero previews directly from decrypted bytes in memory.
  pdfx: ^2.6.0
  # Movie posters come from the backend's TMDB image proxy. Image.network has no disk
  # cache, so every cold start would re-download; the proxy serves posters as immutable,
  # which flutter_cache_manager honours indefinitely.
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Listed file-by-file rather than by directory so an unreferenced asset can
  # never silently start shipping. Every entry below is reachable from
  # lib/core/assets/app_assets.dart or a literal path in lib/features/tickets/.
  assets:
    - assets/branding/docket_logo.svg
    - assets/passes/bookmyshow.svg
    - assets/passes/bookmyshow-logo-vector.svg
    - assets/passes/zomato.svg
    - assets/passes/district-logo.svg
    - assets/passes/history_clock.svg
    - assets/auth/google/android_dark_rd_SI.svg
    - assets/auth/google/ios_dark_rd_SI.svg
    - assets/auth/google/ios_dark_rd_na.svg
    - assets/navbar/ids_filled.svg
    - assets/navbar/ids_unfilled.svg
    - assets/navbar/passes_filled.svg
    - assets/navbar/passes_unfilled.svg
    - assets/navbar/alt/ids_filled.svg
    - assets/navbar/alt/ids_unfilled.svg
    - assets/navbar/alt/passes_filled.svg
    - assets/navbar/alt/passes_unfilled.svg
    - assets/wallet/passport/ashoka_chakra.svg
    - assets/wallet/passport/emblems/emblem_22.png
    - assets/wallet/passport/emblems/emblem_28.png
    - assets/wallet/passport/emblems/emblem_32.png
    - assets/wallet/passport/emblems/emblem_34.png
    - assets/wallet/passport/emblems/emblem_120.png
    - assets/wallet/passport/emblems/emblem_140.png
    - assets/wallet/aadhaar/logo.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
